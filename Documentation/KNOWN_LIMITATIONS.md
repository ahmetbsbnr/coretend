# KNOWN LIMITATIONS
- Built with CommandLineTools only: ad-hoc signature, no notarization, no UI tests.
- Cleanup review list caps display at 5000 findings (memory bound on 8 GB target).
- Symlinks are skipped by scans, not followed.
- Only Cleanup module is functional; other sidebar entries are placeholders and say so.
- Malware scanning not yet present; nothing claims otherwise in the UI.
