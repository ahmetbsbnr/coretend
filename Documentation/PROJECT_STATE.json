{
  "project": "CoreTend",
  "version": "0.8.1",
  "phase": "0.8.1 — CoreTend Rebrand & Workspace Migration: the product is renamed from MacCare Local to CoreTend throughout (sources, targets, bundle identifier com.ahmetbsbnr.coretend, resources, localisations, scripts, workflows, docs, site, legal metadata); pre-rename user data and preferences migrate on first launch, copy-only and reversible; the Living System palette and a complete brand asset set replace the placeholder identity; the product site is rebuilt bilingual on that identity with zero external requests; both repositories now live in the WEBSITE/ workspace as independent git repos. Publication remains blocked: legalReviewStatus is 'pending', no trademark search has been run, and legal identity plus a security contact are still undefined. Local only: no push, no deploy, no public release.",
  "toolchain": "Swift 6.3.2 via CommandLineTools (no Xcode installed)",
  "build_system": "SwiftPM; app bundle assembled by Scripts/package-local.sh",
  "test_framework": "Swift Testing (XCTest unavailable without Xcode); run via Scripts/test.sh",
  "done": [
    "SafetyCore (validator, center, dry-run default, re-validation, audit)",
    "ScanCore (streaming rules, exclusions, min-size/age, matches filter)",
    "DuplicateEngine (size→partial→SHA-256, hard-link aware, keeper suggestion)",
    "SpaceLensEngine + treemap UI (breadcrumbs, list fallback, Other bucket)",
    "SimilarImagesEngine (Vision feature prints, greedy clustering, lazy QuickLook thumbnails, photoslibrary excluded)",
    "Persistence (SQLite actor, migrations, activity/exclusions/settings)",
    "SystemMetrics (CPU/mem/pressure/disk/thermal) + Performance view + LaunchAgent inspection",
    "AppDiscovery (inventory, associated data, leftovers) + uninstall via Trash",
    "MalwareEngine (ClamAV wrapper, honest unavailable state, quarantine restore/delete)",
    "Cloud Cleanup (local vs logical footprint, no downloads, analysis only)",
    "Smart Care orchestrator (dry-run, low-risk-only execution)",
    "Cleanup (7 rules grouped by rule, exclusions honored)",
    "My Activity (history, filters, clear)",
    "Menu bar extra (samples only while open)",
    "Onboarding (real FDA probe)",
    "Settings (dry-run default, exclusions, menu bar toggle)",
    "Docs complete; 0 build warnings",
    "Privacy Cleaner (profiles Chrome/Firefox/Safari, cache-only clean, browser-running warning)",
    "App Updater (App Store receipt / Sparkle feed detection, no downloads)",
    "Accessibility labels on selection toggles",
    "create-test-volume.sh",
    "Orbital Ecology design system (MCColor/MCSpacing/MCMotion/MCFont, composants, Reduce Motion/Transparency)",
    "Core Bloom: icône app native (CG->icns 16-1024), menu bar template, hero Smart Care",
    "Sidebar groupée, Smart Care hero états réels, Performance metric cards, onboarding 4 étapes",
    "Scripts/capture.sh (QA visuelle bundle réel), captures Before/After",
    "Open source foundation: LICENSE/LICENSES/NOTICE/COPYRIGHT/TRADEMARKS/THIRD_PARTY_NOTICES, public README, SECURITY/CODE_OF_CONDUCT/CONTRIBUTING/GOVERNANCE/SUPPORT/DEPENDENCIES, full user+developer docs, .github community files + CI/security workflows, public Scripts/ (bootstrap/doctor/repository-doctor/clean/uninstall-local/check-licenses/check-private-data/check-placeholders)",
    "Reproducible clean-clone build verification (git archive into a fresh temp dir): doctor/test/debug build/release build/package/launch all pass; found and fixed package-local.sh not copying the SwiftPM resource bundle (localization strings) into the .app, which had silently fallen back to a machine-specific absolute .build path",
    "PRIVACY.md added at repo root (was linked from README, previously missing)",
    "Website/ bilingual (en/fr) static site foundation: 13 pages x 2 locales generated by Website/generate.py (stdlib Python, no framework), Orbital Ecology color tokens, zero tracking/analytics/cookies, honest pre-1.0 + no-fake-release status, bracket placeholders for undecided legal/domain/contact info, WEBSITE_ARCHITECTURE/SECURITY/PRIVACY/DEPLOYMENT.md"
  ],
  "partial": [
    "Protection: engine unavailable on this machine (ClamAV not installed) — UI states it honestly; the FSEvents watch layer around it is fully built and tested (off by default, never auto-quarantines)",
    "Cleanup/Smart Care findings list still capped at 5000 rows for display (by design); totals are computed from uncapped stream",
    "Full FR/EN × light/dark × every-module visual-QA capture campaign not run this phase — a real display IS available in this environment (re-confirmed this phase) and one verification capture succeeded, but the sidebar-navigation capture path needs a follow-up fix; see VISUAL_QA.md / KNOWN_LIMITATIONS.md. Code-level checks (tokens, Reduce Motion/Transparency, structural clear/dark) are done."
  ],
  "not_started": [
    "Privileged helper (blocked: no Developer ID signing)",
    "Additional locales beyond en/fr",
    "iOS Simulator / Trash-empty / Mail-attachment / broken-LaunchAgent cleanup rules (deliberately deferred with documented safety rationale — REQUIREMENTS_DECISION_HISTORY.md)",
    "browser history/cookie removal in Privacy Cleaner (deliberately deferred: db corruption risk)"
  ],
  "tests": {
    "passing": 250,
    "failing": 0,
    "suites": 55,
    "note": "Re-run and regenerated 2026-07-24 via Scripts/generate-test-inventory.sh during the 0.8.0 Functional Completion phase."
  },
  "last_build": "Debug + Release both green, 0 project warnings (last Sendable warning in OnboardingView.swift resolved this phase). All dev gates green: doctor, repository-doctor (private-data/licenses/placeholders/feature-inventory/settings-matrix-no-orphan/markdown-links), test-uninstall, test-distribution, test-release-manifest, verify-download.",
  "next_goals": [
    "Capture real Documentation/VisualAudit/After screenshots for all modules on a machine with an attached display, then finish VISUAL_QA.md checkboxes (pure environment follow-up, not a code task)",
    "Resolve Documentation/HUMAN_BLOCKERS.md items (maintainer handle, repo URL, security contact, legal identity, domain) before any real public push or website deploy",
    "Replace website's homepage screenshot placeholder with a real capture before any production deploy",
    "Migrer les vues restantes vers tokens MC* si de nouveaux écrans arrivent"
  ],
  "limitations": [
    "No Xcode: ad-hoc signing only, no UI tests, no notarization",
    "ClamAV optional runtime dependency",
    "helper privilégié bloqué (signature)",
    "No attached display in this sandbox (standing since v0.3.0) — blocks Scripts/capture.sh screenshot capture only; see KNOWN_LIMITATIONS.md"
  ],
  "open_decisions": [],
  "git_commit": "HEAD"
}
