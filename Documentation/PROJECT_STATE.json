{
  "project": "MacCare Local",
  "version": "0.4.1",
  "phase": "3 — Visual Foundation (Orbital Ecology); Step A done, Step B FULLY COMPLETE (all 10 modules); Steps C (fr localization) and D (final 0.5.0 audit) not started",
  "toolchain": "Swift 6.3.2 via CommandLineTools (no Xcode installed)",
  "build_system": "SwiftPM; app bundle assembled by Scripts/package-local.sh",
  "test_framework": "Swift Testing (XCTest unavailable without Xcode); run via Scripts/test.sh",
  "done": [
    "SafetyCore (validator, center, dry-run default, re-validation, audit)",
    "ScanCore (streaming rules, exclusions, min-size/age, matches filter)",
    "DuplicateEngine (size→partial→SHA-256, hard-link aware, keeper suggestion)",
    "SpaceLensEngine + treemap UI (breadcrumbs, list fallback, Other bucket)",
    "SimilarImagesEngine (Vision feature prints, greedy clustering, lazy QuickLook thumbnails, photoslibrary excluded)",
    "Persistence (SQLite actor, migrations, activity/exclusions/settings)",
    "SystemMetrics (CPU/mem/pressure/disk/thermal) + Performance view + LaunchAgent inspection",
    "AppDiscovery (inventory, associated data, leftovers) + uninstall via Trash",
    "MalwareEngine (ClamAV wrapper, honest unavailable state, quarantine restore/delete)",
    "Cloud Cleanup (local vs logical footprint, no downloads, analysis only)",
    "Smart Care orchestrator (dry-run, low-risk-only execution)",
    "Cleanup (7 rules grouped by rule, exclusions honored)",
    "My Activity (history, filters, clear)",
    "Menu bar extra (samples only while open)",
    "Onboarding (real FDA probe)",
    "Settings (dry-run default, exclusions, menu bar toggle)",
    "Docs complete; 0 build warnings",
    "Privacy Cleaner (profiles Chrome/Firefox/Safari, cache-only clean, browser-running warning)",
    "App Updater (App Store receipt / Sparkle feed detection, no downloads)",
    "Accessibility labels on selection toggles",
    "create-test-volume.sh",
    "Orbital Ecology design system (MCColor/MCSpacing/MCMotion/MCFont, composants, Reduce Motion/Transparency)",
    "Core Bloom: icône app native (CG->icns 16-1024), menu bar template, hero Smart Care",
    "Sidebar groupée, Smart Care hero états réels, Performance metric cards, onboarding 4 étapes",
    "Scripts/capture.sh (QA visuelle bundle réel), captures Before/After"
  ],
  "partial": [
    "Protection: engine unavailable on this machine (ClamAV not installed) — UI states it honestly",
    "Cleanup/Smart Care findings list still capped at 5000 rows for display (by design); totals are now computed from uncapped stream, truncation shown honestly in UI (v0.4.1)",
    "Secondary module visual identities — all 10 modules now code-complete (Step B FULLY COMPLETE): Protection mesh (MCMeshView), Cleanup fragments (MCFragmentSpec/MCFragmentView), Space Lens continuity (matchedGeometryEffect + SpaceNodeCategory), Applications capsules (grouping + matchedGeometryEffect over native List), My Clutter overlap (MCOverlapStack shared motif), My Activity timeline (day grouping, real/simulated split), Cloud Cleanup sync state (filled/outline shape), Performance (scenePhase-gated idle pause added this session — chart/metrics already used real SystemMetrics data, no fake curves), menu bar (attention badge via shape/position not color, last Smart Care result, honest ClamAV line, Settings quick action, low-idle-cost 30s background poll for the badge only), Settings (real Full Disk Access/ClamAV/notification/privileged-helper states, real bundle version, Data section with clear-activity-history). Screenshots not captured for any module this session — no attached display in this sandbox (Scripts/capture.sh confirmed failing again, System Events index -1719); all rows stay 'code done, capture pending' in VISUAL_QA.md pending a machine with a real display.",
    "French localization — NOT started (Step C)"
  ],
  "not_started": [
    "Privileged helper (blocked: no Developer ID signing)",
    "Localizations",
    "FSEvents downloads watch",
    "browser history/cookie removal (deliberately deferred: db corruption risk)"
  ],
  "tests": {
    "passing": 82,
    "failing": 0
  },
  "last_build": "Release green, 0 warnings (clean .build); v0.4.1 packaged and launched successfully (non-destructive smoke test, no crash/error in log show) after Performance/menu bar/Settings Step B work — Step B is now FULLY COMPLETE for all 10 modules. 7 new tests (MenuBarIconModel.needsAttention thresholds, PermissionFormatting notification-status labels) in Tests/MacCareAppTests/PerformanceAndSettingsTests.swift.",
  "next_goals": [
    "Step B FULLY COMPLETE (code) for all 10 modules; re-capture Documentation/VisualAudit/After screenshots for all of them on a machine with a display/Accessibility permission, then finish VISUAL_QA.md checkboxes",
    "fr localization — Step C, not started",
    "Migrer les vues restantes vers tokens MC*",
    "Final visual audit -> 0.5.0 — Step D, not started (do not bump version past 0.4.1 until C and D are done)"
  ],
  "limitations": [
    "No Xcode: ad-hoc signing only, no UI tests, no notarization",
    "ClamAV optional runtime dependency",
    "helper privilégié bloqué (signature)"
  ],
  "open_decisions": [],
  "git_commit": "HEAD"
}