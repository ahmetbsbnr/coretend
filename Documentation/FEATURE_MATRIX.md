# FEATURE MATRIX

Status values: COMPLETE, PARTIAL, IMPLEMENTED_UNVERIFIED, BLOCKED_ENVIRONMENT,
BLOCKED_HUMAN, DEFERRED, NOT_STARTED.

Canonical machine-readable source: `Documentation/feature-inventory.json`
(regenerated by `Scripts/generate-feature-inventory.py`, checked by
`Scripts/check-feature-inventory.sh`). This file is the human-readable view
of the same data plus unverifiable-item rationale; if the two ever disagree,
feature-inventory.json wins.

| Module | Status | Notes |
|---|---|---|
| Dashboard | COMPLETE | First sidebar entry (`ModuleID.smartCare` renders `DashboardView`): free space, Trash state, exclusions, recent activity, a large scan action, shortcuts into the other modules. Supersedes the retired standalone Smart Care orchestrator |
| Cleanup | COMPLETE | Cleanup rules include Xcode plus scoped SwiftPM, Homebrew, npm, pnpm and Yarn caches. Dedicated roots are excluded from broad cache scanning; grouped review, explicit confirmation and Trash remain. |
| Integrity | COMPLETE | Native download provenance, code-signature tier and login-item inspection through `IntegrityCore`; read-only, no malware-detection claim or third-party scanner |
| Performance | COMPLETE | Live CPU/memory/pressure/disk/thermal, CPU chart, LaunchAgent inspection (read-only) |
| Applications | COMPLETE | Inventory, associated data, Trash-based uninstall, Leftovers (conservative bundle-id matching, shared/ambiguous detection tested) |
| My Clutter — Large & Old | COMPLETE | Read-only scan, tested |
| My Clutter — Duplicates | COMPLETE | `Sources/ScanCore/DuplicateEngine.swift`, size→partial→SHA-256 staged hashing, hard-link aware, keeper suggestion, wired to `DuplicatesView` |
| My Clutter — Similar Images | COMPLETE | `Sources/ScanCore/SimilarImagesEngine.swift` (Vision feature prints, greedy clustering) wired to `Sources/CoreTendApp/SimilarImagesView.swift`; PhotosLibrary excluded by design. Prior matrix wrongly listed this as absent — corrected this pass after reading Sources/ |
| Space Lens | COMPLETE | `Sources/ScanCore/SpaceLensEngine.swift` wired to `Sources/CoreTendApp/SpaceLensView.swift`: sized tree engine, treemap + list, breadcrumbs, Other-bucket |
| Cloud Cleanup | COMPLETE | Local vs logical footprint per provider; analysis only, no downloads |
| Privacy Cleaner | COMPLETE | `Sources/CoreTendApp/BrowserDetection.swift` + `PrivacyCleanerView.swift` — Chromium-family (Chrome/Edge/Brave/Vivaldi/Chromium) + Firefox + Safari profile detection, cache-only clean to Trash, per-profile block while that browser runs + close-and-rescan. History/cookies/session deletion stays DEFERRED (cache-only) until a proven closed-browser + schema-recognition + backup + tested-restore path exists — sizes are shown, never deleted. Detection extracted to a filesystem-parameterized catalog, tested against fixture home trees |
| App Updater | COMPLETE | `Sources/CoreTendApp/AppUpdatesView.swift` — App Store receipt / Sparkle feed detection, no downloads performed. Prior matrix wrongly listed this as not started — corrected this pass |
| My Activity | COMPLETE | SQLite-backed history (`Sources/Persistence`), filter, clear |
| Menu Bar | COMPLETE | MenuBarExtra, samples only while open |
| Settings | COMPLETE | Exclusions, language, diagnostics and menu bar toggle |
| Onboarding | COMPLETE | Real FDA probe, honest claims, uninstall info |
| Persistence | COMPLETE | SQLite actor, migrations apply-once/idempotent (tested), activity/exclusions/settings storage |
| Storage Timeline | COMPLETE (minimal vertical) | Schema v6 (`timeline_snapshots.scope` added on top of v5's tables — see `Documentation/PERSISTENCE.md`), a `TimelineService` layer (`Sources/CoreTendApp/TimelineService.swift`) between Persistence and UI, a real sidebar screen (`StorageTimelineView` — scope picker, 24h/7d/30d/since-previous windows, biggest increases/decreases, recent history, per-scope and "nothing scanned yet" empty states, clear-history action), and a Dashboard "Since last scan" card, all tested (17 `StoreTests` cases exercise scope isolation, retention, vanished/new categories, overflow, and comparison correctness; see `Documentation/PERSISTENCE.md`). Wired scopes: Cleanup, Duplicates, Leftovers, Privacy — each has a fixed, non-user-parameterized methodology, so two of its scans are a comparable scope. Deliberately **not** wired: My Clutter/Large & Old (size/age thresholds are user-adjustable per scan, so scans aren't comparable), Space Lens (reports an arbitrary folder's total size, a different unit than "reclaimable junk"), Similar Images (its engine only exposes a group's total bytes, not the reclaimable portion excluding the kept image), Cloud Cleanup (reports sync state, not a reclaimable figure). "Minimal vertical" means: this is the first usable end-to-end slice, not every engine CoreTend has — extending to more scopes, or adding physical/APFS-aware sizing, is future work, not a gap in what's shipped here |
| Privileged helper | NOT_STARTED | A Developer ID signing identity is now installed (used for the published v1.0.0), so this is a pure product-scope decision (see `Documentation/CORETEND_MOSCOW_STATUS.md`), not an environment block; no current feature requires it |
| Localizations | PARTIAL | English + French (`Base.lproj`, `fr.lproj`); no other locales |
| Visual QA campaign (After screenshots) | COMPLETE | 44 approved native captures: 11 modules × EN/FR × light/dark, generated 2026-09-04 through isolated capture harness and accepted by maintainer |
| Public release identity (legal name/address, domain, security contact) | COMPLETE | `Configuration/PublicIdentity.local.json` exists (gitignored, real values, verified 2026-07-27); resolution recorded in `Documentation/HUMAN_BLOCKERS.md` → RESOLVED |
| Code signing / notarization | COMPLETE for v1.0.0 | Developer ID Application (NSCUV5G738) signed, Apple-notarized, and stapled; published as `v1.0.0` on 2026-09-03. See `Documentation/RELEASE_STATE.md` |
| CoreTend Advisor | COMPLETE (minimal vertical) | Deterministic, local, read-only explanation layer reused by Developer Center package-cache and Xcode findings. Uses existing `RiskLevel`, `AdvisorConfidence`, `AdvisorReversibility`; explicit FR/EN mappings cover each developer rule. |
| Recovery Plan | COMPLETE (minimal vertical) | Goal → eligible findings → conservative plan → review → user selection → existing safe execution path, end to end. New types (`RecoveryGoal`, `RecoveryPlanCandidate`, `RecoveryPlanCategory`, `RecoveryPlanExclusionReason`, `RecoveryPlanEligibility`, `RecoveryPlanBuilder`, `RecoveryPlanService`) consume `AdvisorFinding` structurally — never Advisor's display text. Eligibility is a real business rule (risk/confidence/reversibility/bytes), not `risk != .high`: high risk is always `.notIncluded` (never auto-selected); `.uncertain` confidence, `.readOnly`, and non-Trash reversibility are excluded with a specific reason; `.probable` confidence and Duplicates' `(medium, .exact)` shape always land in `.reviewRequired`, never preselected. Anti-double-counting: Cleanup's `user.caches` rule structurally overlaps Privacy's browser-cache detection and Leftovers' Caches-location items at the filesystem level (all three read from subtrees of `~/Library/Caches`); with no shared per-file identifier to subtract the overlap, `user.caches` is always `.notIncluded` (`.overlapsAnotherSource`) — shown with its real bytes, never silently dropped, never summed into the plan. Goal accumulation is a deterministic sort-then-stop (risk → category → confidence → bytes → id), never a knapsack search. Execution is sequential per source through each source's own `PathValidator`/`SafetyCenter` — the exact construction `CleanupView`/`DuplicatesView`/`LeftoversView`/`PrivacyCleanerView` already use (`DuplicateSafety.safeSelection` and `PrivacyCleanerViewModel.isRunning` were extracted from those views as shared pure functions so Recovery Plan and the original UI can't drift apart) — never atomic across sources, reported as such. UI: `RecoveryPlanView` (Storage sidebar) with goal presets/custom input, Recommended/Review required/Optional/Not included sections, per-item Advisor rows, and a real confirm-and-execute flow. 31 new tests: `RecoveryPlanTests` (22 cases — eligibility/goal/ordering, pure), `RecoveryPlanExecutionTests` (5 cases against real fixture homes and real `SafetyCenter` Trash moves — not mocked), `DuplicateSafetyTests` (4 cases). Deliberately not done: a Dashboard "potentially recoverable" card (would require either an expensive silent re-scan on every Dashboard load or new persistent caching infrastructure — out of scope this pass, not a forgotten item); Leftovers/Privacy execution use the identical tested `PathValidator`/`SafetyCenter` pattern as Cleanup/Duplicates but aren't separately integration-tested (same architecture, disclosed rather than assumed) |
| APFS Intelligence | PARTIAL (minimal vertical; sub-capabilities below) | Read-only domain layer (`APFSVolumeInspector`/`APFSMetric<Value>`/`APFSIntelligenceService`, `Sources/SystemMetrics/` + `Sources/CoreTendApp/`), zero subprocess usage (Darwin `statfs()` for filesystem type; Foundation `URLResourceKey`s for everything else). Sub-capabilities — **volume capacity/availability: COMPLETE** (`volumeTotalCapacityKey`/`volumeAvailableCapacityKey`/`ForImportantUsageKey`/`ForOpportunisticUsageKey`, shown under their real Apple names, never renamed "Purgeable", never forced into `Total = Used + Available`); **logical vs. physical size: COMPLETE** for Cleanup only (`totalFileAllocatedSizeKey` vs. `fileSizeKey`; `TimelineCategorySample.physicalBytes` now populated for Cleanup snapshots, `nil` for Duplicates/Leftovers/Privacy and all pre-existing snapshots — see `Documentation/APFS_INTELLIGENCE.md` "pairing rule"); **non-APFS detection: COMPLETE** (`statfs`/`f_fstypename`, no APFS-specific UI/logic runs on a non-APFS volume); **clone/hard-link storage-sharing attribution: NOT_STARTED** (no stable public API found to attribute shared bytes to individual files; shown as an explicit, permanent "Unavailable", not estimated); **snapshot listing: NOT_STARTED / PARTIAL** (concept explained, no individual snapshots enumerated — would require this codebase's first subprocess dependency, deliberately not added this pass); **individual snapshot size: NOT_STARTED** (never computed by subtracting two volume measurements). New UI: `APFSIntelligenceView` (Storage sidebar) — volume card, Files Analyzed (reads the existing latest Cleanup Timeline snapshot, never re-scans), Storage Semantics concept cards, Snapshots concept card, Measurement Limitations section. 26 new tests (`APFSVolumeInspectorTests` 9, `LogicalVsPhysicalSizeTests` 7, `ScanFindingAggregationTests` 4, `APFSIntelligenceServiceTests` 6). Zero destructive code paths — no snapshot deletion, no volume modification, no privileged operation, anywhere, including unwired/future-use code. Not eligible for Recovery Plan by construction: no `AdvisorFinding` is created for any APFS concept (see `Documentation/SAFETY_MODEL.md` → "APFS Intelligence"). Also found and documented (not fixed, to avoid an unaudited refactor of production scan paths): `ScanEngine`/`SpaceLensEngine`/`AppDiscovery.directorySize` do not deduplicate hard links by inode the way `DuplicateEngine` already does — see `Documentation/APFS_INTELLIGENCE.md` "Pre-existing hard-link double-counting" |
| Applications Center 2.0 | PARTIAL (read-only inspection depth added; sub-capabilities below) | `ApplicationInspection`/`ApplicationInspectionService` (`Sources/CoreTendApp/ApplicationInspection.swift`), a per-app, on-demand (never whole-list) read-only composition over existing `AppDiscovery`/`IntegrityCore` engines — zero new subprocess usage. Sub-capabilities — **identity (bundle id/version/running): COMPLETE** (`InstalledApp`, unchanged, plus `ApplicationRuntimeState` via `NSWorkspace`); **storage breakdown: COMPLETE** (per-kind totals + "Known associated storage", never "Total" — see `Documentation/APPLICATIONS_CENTER.md`); **associated item confidence: COMPLETE** (reuses `AdvisorConfidence`; exact-bundle-id items are `.exact`, Group Container vendor-prefix matches are `.probable` and flagged `isShared` when more than one installed app shares the vendor prefix); **installation source vs. update mechanism: COMPLETE as two separate types** (`InstallationSource`/`UpdateMechanism` — a Sparkle-updated, directly-downloaded app correctly shows both facts, never "Installed via Sparkle"); **code signing/provenance: COMPLETE** (connects the pre-existing `IntegrityCore.CodeSignInspector`; quarantine-based provenance only — `ProvenanceScanner`'s richer per-file origin fields are not wired to the per-app view this pass); **architecture: COMPLETE** (existing arm64/x86_64/universal detection, unchanged); **universal binary slice sizes: COMPLETE for the standard 32-bit `fat_arch` form** (real arm64/x86_64 macOS binaries), **NOT_STARTED for `FAT_MAGIC_64`** (documented, not silently mishandled — fails closed to `nil`); **launch items per app: COMPLETE** (`IntegrityCore.LoginItemScanner` + a reliable-signal-only association: in-bundle program path or bundle-id-matching Label; a name-only resemblance is never surfaced at any confidence); **receipts/PKG installer detection: NOT_STARTED** (would require a `pkgutil` subprocess, avoided for the same reason as APFS Intelligence's snapshot listing); **search/filter extensions (by architecture, installed-via, running, etc.): NOT_STARTED** (existing name search unchanged; judged not worth the added surface this pass); **destructive extensions (launch item disable/enable, binary thinning): NOT IN SCOPE** — read-only by design, not deferred by omission. New UI: four new detail sections (Header facts, Storage, Security & Provenance, Startup) in the existing `ApplicationsView`, loaded lazily and cancellably per selection; the pre-existing Associated Data list and its uninstall Toggle are unchanged for exact-bundle-id items, with Group Container candidates appended for visibility only (never selectable in this pass). 42 new tests (12 `AppDiscoveryTests` install-source/vendor-prefix/Group-Containers, 3 more hardening cases for malformed/missing Info.plist and duplicate bundle IDs, 27 `ApplicationInspectionTests` across 6 suites — UniversalBinaryAnalyzer, AssociatedItemConfidence, ApplicationStorageBreakdown, LaunchItemAssociator, ApplicationRuntimeInspector, AssociatedItemAdvisory). Zero destructive code paths added. Not eligible for Recovery Plan by construction: `RecoveryPlanService` was not modified and carries zero references to any new type (see `Documentation/SAFETY_MODEL.md` → "Applications Center") |
| Developer Center | COMPLETE (minimal vertical) | Real Developer Center UI with Xcode, package caches, read-only CoreSimulator and Docker sections. Existing Xcode rule IDs reused; package rules cover SwiftPM, Homebrew, npm `_cacache`, known pnpm v3/v10 content/index paths, Yarn Classic and review-required Yarn Berry. Scan is cancellable/session-cached; action routes through CleanupExecution → PathValidator → SafetyCenter → Trash/journal. Simulator Cleaner and Docker Cleanup remain NOT IN SCOPE. |

## Unverifiable-here items and why

- **Live scan / on-screen accessibility behavior**: interactive VoiceOver,
  keyboard, focus, Dynamic Type, Reduce Motion/Transparency and visual matrix
  passed by maintainer attestation on 2026-09-04. Automated tests remain
  complementary evidence, not substitute observation.
- ~~Integrity team-signature tier: skipped~~ — corrected 2026-09-05: a
  Developer ID identity has been installed since 2026-08-31, so
  `teamSignedBinary()` now runs and passes for real rather than being
  skipped (verified by grepping the actual test-run log). Apple-signed,
  unsigned and ad-hoc tiers are covered too, without fabricating an
  identity.
