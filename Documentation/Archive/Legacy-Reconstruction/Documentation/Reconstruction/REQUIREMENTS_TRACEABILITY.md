# Cahier de reconstruction — matrice de traçabilité

Référence: `docs/superpowers/specs/2026-09-25-coretend-reconstruction-design.md`. États décrivent ce checkout et ses preuves connues, pas la cible. `IMPLEMENTED_UNVERIFIED` signifie code repéré sans preuve suffisante; `PARTIAL` signifie implémentation/preuve incomplète; `GAP` signifie absence ou contradiction bloquante. `BLOCKED_DECISION` exige arbitrage mainteneur.

## Exigences fonctionnelles

| ID | Priorité | État | Implémentation actuelle | Preuve | Écart / prochaine action | Programme |
| --- | --- | --- | --- | --- | --- | --- |
| FR-01 | M | IMPLEMENTED_UNVERIFIED | `MainWindow` restaure/persiste `ModuleID` via `ModuleSelectionPreference`; README/spec/sidebar alignés sur onze destinations | `ModuleSelectionPreferenceTests.swift` (round-trip, fallback, suite isolée); `SidebarReachabilityTests.swift`; `CommandPaletteTests.swift`; parcours UI/XCUITest préparé mais non exécuté | Contrat de préférence, labels et reachability prouvés; restauration après relance réelle reste non vérifiée sans runner GUI natif. | P0, P1, P3 |
| FR-02 | M | VERIFIED | `Sources/ScanCore/ScanCore.swift`; engines typed in `Sources/ScanCore/` | `ScanEngineTests.swift`, `AnalysisReportEngineTests.swift`, `LegacyStreamContractTests.swift`, `ScanConcurrencyTests.swift` | Typed partial/cancel/limits and one terminal event per compatibility stream covered. UI runtime cancellation/progress still needs manual release QA. | P0, P1 |
| FR-03 | M | PARTIAL | `FindingMetadata.swift`; typed issue/measurement reports from ScanCore and inventory modules | `FindingMetadataTests.swift`, `AnalysisReportEngineTests.swift`, `AppDiscoveryTests.swift`, `IntegrityCoreTests.swift` | Engine status, issue code and byte provenance covered. Full on-screen explanation of every issue/limit still needs manual UX review. | P1 |
| FR-04 | M | VERIFIED | Exclusions dans `Sources/Persistence/Store.swift`, `SettingsView.swift`, `ClutterFiltering.swift` | `StoreTests.swift`; `ClutterFilteringTests.swift` | Confirmer changement de périmètre immédiatement visible dans parcours UI. | P1 |
| FR-05 | M | PARTIAL | `Sources/SafetyCore/SafetyCore.swift`; six vues d’action | `PathValidatorTests.swift`: protected roots, allowlist, symlink swap, durable approval; fake Trash success and failure fixtures | Production path uses only `FileManager.trashItem`; failure leaves source in place. UI confirmation/runtime remains unobserved without native GUI gate. Copy mismatch reclaimed-space closed by P1/P4 gates. | P1, P5 |
| FR-06 | M | PARTIAL | `Sources/Persistence/Store.swift`; `MyActivityView.swift`; `SafetyLogView.swift` | Persistence 57/57; refusal, audit failure, terminal-event and redaction cases in SafetyCore tests; outcomes counted by operation type | Approval is refused without durable audit; migration/idempotency covered. Full native walkthrough of refusal, failure and success across every destructive surface remains unobserved. | P1, P3, P5 |
| FR-07 | M | VERIFIED | `Sources/ScanCore/DuplicateEngine.swift`; `DuplicatesView.swift` | `DuplicateEngineTests.swift`; `SimilarImagesGroupTests.swift`; `DuplicatesFilterTests.swift` | Preuve d’invariant testée; compléter parcours visuel/revue avant version cible. | P1 |
| FR-08 | M | PARTIAL | `SpaceLensEngine.swift`; `SpaceLensView.swift`; `CloudFile.swift` | `SpaceLensTests.swift` (unknown root, depth/visit/result caps, placeholder with nil allocation), `CloudCleanupTests.swift` (local total excludes 5 GB remote logical bytes) | Engine and local-byte invariants covered; manual review still needed for all visual states and real iCloud provider behavior. | P1 |
| FR-09 | M | PARTIAL | `IntegrityCore.swift`; `ProtectionView.swift`; `DiagnosticReport.swift` | `IntegrityCoreTests.swift` (typed missing roots, symlink plist rejection, native signature unknown/validity), `AnalysisPresentationTests.swift` | Native signals and unknown signature state covered; do not claim antivirus coverage. Manual permission-denied UX review remains. | P1 |
| FR-10 | M | PARTIAL | `OnboardingLogic.swift`; `OnboardingView.swift` | `OnboardingLogicTests.swift`; `CoreTendAccessibilityTests` (2 tests: annonces EN/FR et hints pause) | Les probes FDA/permissions réels, guidance système et mode dégradé n’ont pas été observés sur runtime macOS. | P1, P3 |
| FR-11 | M | PARTIAL | `Sources/Persistence/Store.swift`; `LegacyDataMigration.swift` | PersistenceTests (57/57, 2026-09-26), dont `migrationsApplyOnceAndAreIdempotent` et `rollbackRemovesOnlyWhatThisRunCreated` | Migration repeatable/rollback legacy couverts; sauvegarde/restauration utilisateur et procédure de recovery complète absentes. | P1, P5 |
| FR-12 | M | PARTIAL | resources `Base.lproj`/`fr.lproj`; CI copy gate; site `Website/` | 585/585 key parity and `check-copy-honesty.py` pass on 2026-09-26 | Key parity and critical safety copy pass; human review of wording and website coverage remains. | P0, P1 |
| FR-13 | M | VERIFIED | `Sources/CoreTendCLI/main.swift`; `Documentation/COMMANDS.md` | Local CLI run: help exit 0; 10 tab-separated rule rows; 7 absolute roots; unknown option exit 1; source exposes only read-only rule/root enumeration | No file-mutating or privilege APIs in CLI; verified on this host. | P1 |
| FR-14 | S | PARTIAL | `ClutterControls.swift`; `ClutterFiltering.swift`; `DuplicatesView.swift` | `ClutterFilteringTests.swift`; `DuplicatesFilterTests.swift` | Filtres présents; presets reproductibles, règle/unité expliquées à confirmer. | P1 |
| FR-15 | S | GAP | Aucun planificateur de scan identifié dans sources/inventaire courant | Recherche de `schedule`/`LaunchAgent` montre seulement inspection en lecture | Ajouter uniquement si décision produit confirme; toute planification demeure lecture seule. | P2 |
| FR-16 | S | IMPLEMENTED_UNVERIFIED | `homebrew/coretend.rb`; `Scripts/generate-homebrew-cask.py` | gate du doctor compare cohérence locale selon Task 2 | Cask configuré; version publique/checksum inconnue sans comparaison aux octets publiés. | P0, P2 |
| FR-17 | C | GAP | Locales Base/EN et FR seulement | `FEATURE_MATRIX.md`; clés de ressources | Aucune langue additionnelle engagée; choisir langue et revue humaine si demande confirmée. | P3 |
| FR-18 | C | GAP | `UpdateChecker.swift` vérifie manifeste; pas de flux d’installation identifié | `UpdateCheckerTests.swift`; README précise aucun téléchargement | Updater installant exige modèle signature/rollback/autorisation séparé. | P3 |

## Exigences non fonctionnelles

| ID | Priorité | État | Implémentation actuelle | Preuve | Écart / prochaine action | Programme |
| --- | --- | --- | --- | --- | --- | --- |
| NFR-01 | M | PARTIAL | `SafetyCore.PathValidator`, `SafetyCenter` unique exécuteur de déplacement | `PathValidatorTests.swift`: protected roots, symlink swap, durable approval, Trash failure preserves fixture bytes; source search reviewed | Aucun bypass production ni fallback permanent détecté. Race/file-identity coverage ne prouve pas chaque entrelacement système; runtime natif non observé. | P1, P5 |
| NFR-02 | M | VERIFIED | `SafetyCore.SafetyCenter`; app/site copy gates | SafetyCore appelle uniquement `FileManager.trashItem`; régression prouve que l’échec préserve la source; copy honesty et browser gate passent | Code local et gates copy vérifiés; restauration via interface Corbeille macOS non observée manuellement. | P1, P4, P5 |
| NFR-03 | M | PARTIAL | `UpdateChecker.swift` fait un GET HTTPS sur action utilisateur; données de scan et audit restent locales | `UpdateCheckerTests.swift`; recherche `URLSession` dans Sources; `Documentation/PRIVACY.md`; gates private-data/test-isolation | Revue source identifie ce seul client réseau et aucun envoi de données produit; métadonnées ordinaires de connexion peuvent être visibles du serveur; audit indépendant absent. | P1, P5 |
| NFR-04 | M | PARTIAL | `Persistence/Store.swift`, événements SQLite, migration legacy allowlistée | PersistenceTests (57/57); `LegacyDataMigrationTests.rollbackRemovesOnlyWhatThisRunCreated` | Tests ciblés réussis; migration rollback ne couvre que créations de migration. Aucun backup/restore utilisateur; rétention, expurgation des chemins et recovery complet restent à décider/prouver. | P1, P5 |
| NFR-05 | M | PARTIAL | Typed reports in ScanCore, AppDiscovery, IntegrityCore; UI partial states | `AnalysisReportEngineTests.swift`, `SpaceLensTests.swift`, `AppDiscoveryTests.swift`, `IntegrityCoreTests.swift`, `LegacyStreamContractTests.swift`, `AnalysisPresentationTests.swift` | Missing-root, content failure, caps, cancellation and stream terminal coverage present. Deterministic child metadata failure and large-hash chunk cancellation now pass; synthetic duplicate-engine fixture proves remote placeholder is excluded before hashing; live iCloud provider behavior remains untested. | P1 |
| NFR-06 | M | IMPLEMENTED_UNVERIFIED | `Tests/CoreTendPerformanceTests/ScanPerformanceTests.swift`; `Scripts/measure-stability.sh`; bounded ScanCore fixtures | Filter 300 files: 52 ms; stability snapshot: process-live 25 ms, idle RSS 111→107 MiB/60 s, 100 cycles +1 MiB RSS (2026-09-26, arm64/macOS 27.0) | One host and synthetic fixture only; process-live is not first-window latency. No representative workload matrix or accepted budget; D-06 open. | P2, P5 |
| NFR-07 | M | PARTIAL | `DesignSystem/AccessibilityState.swift`; identifiants et labels SwiftUI | `AccessibilityContractTests.swift` (2 tests; clés critiques résolues EN/FR); captures natives et XCTest UI indisponibles sur ce runner | Vérifier clavier, VoiceOver, zoom, contraste, mouvement et focus dans session GUI interactive; l’attestation historique 1.0.0 ne couvre pas ce checkout. | P1, P3 |
| NFR-08 | M | PARTIAL | `OnboardingView.swift`, états vides/permission à travers l’UI | `OnboardingLogicTests.swift`; test XCUITest skip/reprise préparé mais non exécuté; capture native non obtenue | Observation du parcours de première analyse et du cas permission refusée absente pour checkout actuel. | P1, P3 |
| NFR-09 | M | PARTIAL | `Package.swift` macOS 14; distribution arm64 déclarée | P5 host/build/tests: arm64, macOS 27.0, Swift 6.4; Release bundle build pass | Une seule version hôte validée; aucune matrice OS/matériel. macOS 14+/arm64 reste déclaration de support, pas validation de compatibilité. | P0, P2, P5 |
| NFR-10 | M | PARTIAL | SwiftPM targets; `AppDiscovery` and `IntegrityCore` now depend on `ScanCore` contracts | `Package.swift`; Release `CoreTendApp` target build; `ARCHITECTURE_OVERVIEW.md` | Dependency direction verified. `CoreTendApp` remains a broad executable target; no artificial split attempted. | P1 |
| NFR-11 | M | PARTIAL | dépendance externe `swift-testing` portée par targets de tests | `Package.swift`; build release local | Vérifier archive/app empaquetée et SBOM avant affirmation sur artefact; inventaire des scripts/outils dev distinct. | P2 |
| NFR-12 | M | PARTIAL | `Scripts/sync-published-release.sh` + générateur; site fail-closed | `test_published_release.py` (8 fixtures), `test_website_release_mode.py` (3 fixtures), tests offline pass | Flux vérifie signature puis octets DMG/ZIP avant changement; release distante demeure UNKNOWN car aucune synchronisation réseau n’a été faite. | P0, P4 |
| NFR-13 | M | PARTIAL | cahier et baseline créés; index docs existants | `repository-doctor.sh` passe le 2026-09-26; zéro lien Markdown interne brisé | Index et dérivations vérifiés automatiquement; revue humaine complète des conflits historiques et preuves reste à effectuer. | P0 |
| NFR-14 | M | PARTIAL | générateur `Website/`; CSP/first-paint checks | `Scripts/check-website.sh` 32/32, incluant Axe, langues, reduced motion, routes et crawl interne; doctor passe | Build navigateur local prouvé; déploiement distant et revue humaine du site restent non vérifiés. | P0, P2 |


## Programme 4 — public integration (2026-09-26)

| Requirement | Evidence | Status / limit |
|---|---|---|
| Public release facts must be independently verified before display or download redirect | `Scripts/generate-published-release.py`, `Tests/ScriptTests/test_published_release.py`, `Website/build.py`, `Tests/ScriptTests/test_website_release_mode.py` | Offline fixtures pass; live GitHub release was not queried. Current local record remains unverified. |
| Localization key parity must be enforced | `Scripts/check-localization-parity.py`, `Tests/ScriptTests/test_localization_parity.py`, CI and repository-doctor | 585 English and 585 French keys; malformed, duplicate, and drift cases covered. |
| Public copy must match current product and avoid unsupported reclaimable-space claims | `Website/index.html`, `Website/build.py`, copy-honesty gate | Updated; verify with static site gates. |


## Programme 5 — hardening (2026-09-26)

| Exigence | Preuve ajoutée | État |
|---|---|---|
| NFR-03 privacy/network boundary | Recherche source `URLSession`, `Process`, appels d’ouverture; `PRIVACY.md`; `SECURITY_AUDIT_CURRENT.md`; private-data et test-isolation gates passent | PARTIAL: seul GET identifié, mais revue indépendante et observation réseau non faites. |
| FR-11/NFR-04 recovery | PersistenceTests 57/57; `LegacyDataMigrationTests.rollbackRemovesOnlyWhatThisRunCreated` | PARTIAL: migration legacy rollback couverte; aucune sauvegarde/restauration utilisateur. |
| NFR-06 performance | Scan fixture 300 fichiers 52 ms; stability snapshot 25 ms process-live, RSS idle 111→107 MiB/60 s, 100 cycles +1 MiB | IMPLEMENTED_UNVERIFIED: un hôte arm64/macOS 27.0 uniquement; process-live n’est pas première fenêtre; budgets représentatifs absents, D-06 ouvert. |
| NFR-09 compatibility | `sw_vers`, `uname -m`, `swift --version`; build Release local | PARTIAL: un hôte arm64/macOS 27.0/Swift 6.4 uniquement. Autres OS/architectures non vérifiés. |
| Uninstall isolation | `Scripts/test-uninstall.sh` — 7 scénarios passent en fake HOME | PASS pour allowlist, dry-run, symlink et choix legacy dans ce fixture. Pas une validation d’installation/upgrade GUI. |
