# Functional Completion Execution Plan (0.8.0)

Tracks the 21-step "0.8.0 — Functional Completion" effort. Status is honest:
DONE means real code + passing tests exist; PARTIAL means some real work landed
but the step is not fully satisfied; TODO means not started this phase.
The product version stays **0.7.1** until every automatable criterion in step 19
is genuinely true — it is not yet.

| # | Task | Status | Commit | Notes |
|---|------|--------|--------|-------|
| 0 | Verify state + baseline gates | DONE | 0c1394d | 118→122 tests, 0 warnings, Debug+Release build, all dev gates green, clean tree |
| 1 | Cleanup finalization | PARTIAL | (this) | Added old-installers, old-archives, Xcode-archives rules (Downloads/Developer-scoped, never preselected) + 4 tests. Simulators / Trash-empty / Mail attachments / broken LaunchAgents = DEFERRED (need dedicated safety engines, not blind extension rules) |
| 2 | Smart Care full audit | DONE | 846617c | Audited orchestrator against safety matrix (SMART_CARE_AUDIT.md). Extracted pure `autoExecutableFindings` (only low-risk+preselected auto-executes) + SmartCareSafetyTests + catalog invariant (no preselected medium/high rule). Module *implementations* for Protection/Performance/Applications remain Steps 3/5 (honestly shown unavailable) |
| 3 | Protection / FSEvents watch | DONE | 28540dc | `ProtectionWatcher` actor behind injectable `MalwareScanning`/`FileProbe`: debounce/coalesce, file-stability wait, size+mtime fingerprint dedup with disk persistence, rate limit, honest ClamAV-unavailable, clean stop/restart. NEVER auto-quarantines (notify→review→voluntary). Real `FSEventsProducer` bridges FSEventStream→AsyncStream (not unit-tested; needs live runloop). Wired into Protection UI as off-by-default in-session watch over Downloads/Applications. +14 tests (139→153): single/burst/repeat/temp/modified/deleted/unmounted/unavailable/disabled/cancel/restart/persist |
| 4 | Privacy Cleaner honesty | DONE | (this) | Extracted testable `BrowserCatalog.detect(home:)` — real non-fuzzy detection of Chromium family (Chrome/Edge/Brave/Vivaldi/Chromium, "Default"/"Profile N" layout) + Firefox + Safari. Default stays cache-only to Trash; per-profile running-browser block + close-and-rescan already present. History/cookies/session deletion kept DEFERRED honestly (sizes shown, never deleted) — no closed-browser+backup+restore path yet. +5 fixture-tree tests (153→158) |
| 5 | Applications & updates | DONE | (this) | Centralized update-mechanism detection into tested `AppDiscovery.updateMechanism` engine (`UpdateMechanism` enum): App Store receipt, safe-https Sparkle feed (rejects http/file/javascript/no-host as dangerous), download-origin `kMDItemWhereFroms`, honest `.unknown`; non-overpromising `actionLabel` ("Show Update Options", never "Available"). UI `AppUpdateSource.detect` now delegates (single source of truth). +9 tests. Homebrew Cask origin NOW IMPLEMENTED (was deferred): found a concrete non-fuzzy signal — Caskroom `<token>/.metadata/<ver>/<ts>/Casks/<token>.json` carries an exact `{"app":["Name.app"]}` artifact stanza (incl. rename `target`). New `HomebrewCaskIndex.build()` maps exact bundle name→token; `classify` gains `.homebrewCask(token:)` outranking Sparkle/manual (App Store still wins). UI adds `.homebrew` update source (reveal-in-Finder action; never shells out to brew). +8 tests |
| 6 | My Clutter | PARTIAL | (this) | Full checklist audit of all three sub-modules. **Large & Old**: verified read-only (reveal/Quick Look only, no deletion path) — size/age filters, size/age sort, 2000-cap, graceful error handling confirmed; +sortedFindings seam test. **Duplicates**: verified pipeline (size→64KB partial→SHA-256, hard-link collapse, symlink+remote-only-iCloud skip, shallowest keeper, wastedBytes excludes keeper, keeper never fully removable, Trash-based); FIXED mid-scan-edit guard (`hasChangedOnDisk`, fresh-URL read to defeat URL resource-value caching) so an edited copy is deselected before trashing. **Similar Images**: verified Vision pipeline, format/EXIF/corrupt/memory-bounds/async-thumbnail/threshold/best-pick; verified NO deletion path (reveal-only → never auto-deletes); +end-to-end engine test. +6 tests (185→191). DEFERRED (enhancement, not safety): Large&Old name search, per-volume awareness, UI-exposed exclusions; sparse-file allocated-gain; view `.unavailable` branch unreachable (harmless). |
| 7 | Space Lens | TODO | | |
| 8 | Cloud Cleanup | TODO | | |
| 9 | Settings matrix | DONE | 995a49b | dryRunDefault orphan fix (e0021fa) + `Scripts/generate-settings-matrix.py`: derives SETTINGS_MATRIX.md from settings-matrix.json (5 settings), discovers real keys from Sources/ and fails on any orphaned/undocumented setting — no-orphan gate wired into repository-doctor. Also fixed a flaky 5s ClamAV process-timeout test (→30s) |
| 10 | macOS compatibility audit | DONE | (this) | Re-ran full API grep across Sources/ (imports, MenuBarExtra, Vision, FSEvents, ubiquitous-item keys, QuickLook, SQLite3, systempreferences deep-links, @Observable×18). Every API is at or below the macOS 14 floor: MenuBarExtra/NavigationSplitView=13, Vision feature-print=10.15, QLThumbnailGenerator=10.15, FSEvents/ubiquitous/SQLite3=ancient, @Observable=14 (exactly at floor, the binding constraint). No API exceeds the target ⇒ **zero `@available` guards needed** (adding them would be dead defensive code per scope rules). No ServiceManagement/SMAppService present. Confirms existing `API_AVAILABILITY_AUDIT.md`. Added `.github/workflows/compat-matrix.yml` (macos-14+macos-15 build+test matrix) = IMPLEMENTED_UNVERIFIED (never pushed/run here; no Xcode/2nd Mac). Static grep audit only — not compiled against the 14.0 SDK (single dev machine is macOS 26). |
| 11 | Stress tests | DONE | (this) | 9 synthetic stress fixtures (191→200 tests) in the normal suite; count assertions + generous wall-clock ceilings, durations printed not asserted. Cleanup 12k findings (uncapped streaming, exact totals — extends the 5001 case). Duplicates 10,200 files worst-case single size bucket (9k unique + 500 dup pairs + 100 hard-link pairs + 2 sparse): exactly 500 groups, hard links collapse, sparse doesn't crash hasher, ~linear (45s ceiling catches O(n²)). Similar Images 157 incl. 4×5000²: metadata-only pixelCount asserted memory-bounded. Space Lens wide 5k siblings + deep 60 levels (no overflow, bytes summed past depth cap). SQLite history 8k×2 rows: **read** perf measured (~0.01s, index-backed LIMIT). FSEvents burst 20k events/50 paths → exactly 50 scans (coalesce, memory bounded by distinct paths). Rapid cancellation of Space Lens + ScanEngine (fast teardown). **No bugs found.** Duration measurable; CPU/mem qualitative (metadata-only / bounded-key structural proof, no fabricated bytes). Suite ~7s→~12s real. See STRESS_TEST_REPORT.md |
| 12 | Accessibility | CODE_DONE (interactive VoiceOver = BLOCKED_ENVIRONMENT) | (this) | Code-level a11y audit of every SwiftUI view in MacCareApp + shared DesignSystem views. Found the codebase already strong (labeled toggles, combined elements, Reduce Motion/Transparency/Differentiate-Without-Color + MCAccessibilityState.increaseContrast wired into DesignSystem, treemap child-list keyboard alternative, symbol+text status badges, text keeper badge in Duplicates, icon+color threat rows in Protection, rich accessibility labels on Applications/CloudCleanup/SimilarImages rows). Fixes (4 atomic commits, all modifier-only unless noted): (1) reveal-in-Finder icon buttons in Cleanup/Protection/Duplicates/Leftovers/Performance now carry `accessibilityLabel` (MyClutter/CloudCleanup/SimilarImages already had them); (2) Privacy per-profile selection toggle labeled; (3) Space Lens treemap now `accessibilityElement(children:.ignore)` + item-count/size summary label so VoiceOver reads a summary not dozens of unlabeled rectangles (child list stays the keyboard-navigable equivalent); (4) menu-bar metric rows add a non-color warning glyph + combined "needs attention" VoiceOver label (CPU/free-space warns were color-only); (5) MCEmptyState/MCErrorState decorative icons `accessibilityHidden`. New EN/FR strings: spacelens.treemap.a11y_summary, menubar.warning_a11y. No functional/safety behavior changed. Determinate ProgressView(value:total:) auto-exposes % to VoiceOver; indeterminate spinners pair with a readable count/label. **Interactive VoiceOver + real-display verification stays BLOCKED_ENVIRONMENT** (no display/VoiceOver session in this headless CommandLineTools environment). SwiftUI view-modifier presence is not unit-testable, so these are modifier-only with no new tests. |
| 13 | Installer & first-run wizard | DONE (view-only steps not unit-testable) | (this) | Extended OnboardingView into the 8-step plan wizard. **Tested logic** (`OnboardingLogic.swift`, +15 tests, 200→215): SecurityProfile→SecurityConfig with safe-default invariant (no profile enables medium-risk/empty-Trash/auto-quarantine); LaunchLocation.detect (Applications/Downloads/DMG/temp+AppTranslocation/other + canOfferMove); SystemCheck.items/overall (per-item + worst-wins: unavailable>actionRequired>limited>ok); ClamAVVersionInfo.parse (clamscan --version engine/signatures). **View-only (no test — SwiftUI layout)**: all 8 steps — Welcome (name/local/no-account/no-telemetry/open-source/version/unsigned + move-to-Applications banner), Security profile (3 profiles, editable dry-run in Custom, other flags shown read-only = app's fixed safety posture), File access (live FDA + open-Settings/recheck, "app can't grant this itself" copy), Optional protection (ClamAV available/redacted-path/version/signatures via best-effort probe + FSEvents availability, no auto-install), Menu bar & notifications (opt-in toggles; enabling requests real macOS auth, never claims grant), Folders & exclusions (pick scannable, note protected, add exclusions), System check (live gatherCheckInputs → non-destructive self-diagnostic), Summary (exact enabled options + privacy/restore + docs link + Start persists dryRunDefault/securityProfile/exclusions). Move-to-Applications = user-space FileManager copy to /Applications (fallback ~/Applications, then reveal-for-drag) — no sudo/privilege escalation/password. Re-launchable from Settings ("Run setup assistant again" → .mcShowOnboarding). EN+FR strings, keyboard default action, decorative icons a11y-hidden. New persisted setting `securityProfile` documented in settings-matrix.json. **Installer/DMG packaging itself = DEFERRED** (this slice is the first-run wizard only; no 0.8.0 zip built per scope). Interactive on-display verification = BLOCKED_ENVIRONMENT. |
| 14 | Installer/app animation | TODO | | |
| 15 | Visual validation | TODO | | needs display; else BLOCKED_ENVIRONMENT |
| 16 | Local site copy | TODO | | |
| 17 | Documentation | PARTIAL | (this) | CLEANUP_GUIDE updated for new rules |
| 18 | Local CI gates | DONE | 0c1394d | dev gate green; publish gate blocked (expected, pre-release) |
| 19 | Version 0.8.0 decision | BLOCKED | | criteria not met — do NOT bump |
| 20 | Final artifacts | BLOCKED | | only after 19 |
| 21 | Final audit package | BLOCKED | | only after 19 |

## Resume

Read this file + `CONTINUATION.md` + `CURRENT_PROJECT_STATE.json` first, then
pick the next TODO row. Each slice: implement → `bash Scripts/test.sh` →
`swift build -c release` → inspect diff → atomic local commit (never push) →
update this table's Status/Commit → update CONTINUATION.md → continue.
