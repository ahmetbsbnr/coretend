# Settings Matrix — generated, do not hand-edit

Source of truth: `Documentation/settings-matrix.json`. Regenerate with
`Scripts/generate-settings-matrix.py`. The generator also fails if the
declared settings do not exactly match those present in `Sources/`
(no orphaned or undocumented public setting).

| ID | Label | Default | Storage | Type | Consumer(s) | Effect | Restart | Availability | Test | Status |
|---|---|---|---|---|---|---|---|---|---|---|
| menuBarEnabled | Menu bar item enabled | true | @AppStorage (UserDefaults) | Bool | CoreTendApp.swift (MenuBarExtra isInserted), SettingsView.swift toggle | Inserts/removes the Core Bloom menu-bar item live. | no | always | generate-settings-matrix.py --check (repository-doctor gate) | VERIFIED_COMPLETE |
| appLanguage | App language override | system | @AppStorage (UserDefaults) | String (AppLanguage.rawValue: system/en/fr) | L10n.swift (LocalizationManager), CoreTendApp.swift (.id() on the window + menu-bar content), SettingsView.swift picker, OnboardingView.swift Welcome-step picker | Overrides which .lproj bundle L() looks strings up in, independent of the system locale; "system" preserves the original Bundle.module resolution exactly. | no | always | LocalizationManagerTests.swift | VERIFIED_COMPLETE |
| onboardingDone | Onboarding completed | false | @AppStorage (UserDefaults) | Bool | CoreTendApp.swift MainWindow (gates first-run onboarding sheet) | When false, the first-run onboarding is shown on launch; set true on completion. | no | always | generate-settings-matrix.py --check (repository-doctor gate) | VERIFIED_COMPLETE |
| onboardingStep | Onboarding step index | 0 | @AppStorage (UserDefaults) | Int | OnboardingView.swift (current wizard step) | Persists progress through the onboarding wizard so it resumes at the same step. | no | always | generate-settings-matrix.py --check (repository-doctor gate) | VERIFIED_COMPLETE |
| exclusions | Excluded paths | [] (empty) | Persistence Store exclusions table (path list) | [String] (paths) | CleanupView.swift, SmartCareView.swift (filters findings), SettingsView.swift (add/remove), DiagnosticReport.swift (count) | Paths the user has excluded are never included in cleanup/care findings. | no | always | generate-settings-matrix.py --check (repository-doctor gate) | VERIFIED_COMPLETE |
| securityProfile | Security profile | recommended | Persistence Store settings table (key/value) | String (recommended/cautious/custom) | OnboardingView.swift (persists the first-run wizard profile choice) | Records which security profile the user picked at first run. Informational; every profile keeps the fixed Trash, protected-root and explicit-confirmation safety posture. | no | always | OnboardingLogicTests (profile→config mapping), generate-settings-matrix.py --check (repository-doctor gate) | VERIFIED_COMPLETE |
| notif.enabled.lowDiskSpace | Notification: low disk space | true | @AppStorage (UserDefaults) | Bool | SettingsView.swift toggle; NotificationService.swift (NotificationPreferences) gates the low-disk-space local notification | When off, CoreTend never sends the low-disk-space local notification. Delivery still also requires granted notification permission. | no | always | NotificationServiceTests.swift (aDisabledCategoryNeverPosts), generate-settings-matrix.py --check (repository-doctor gate) | VERIFIED_COMPLETE |
| notif.enabled.scanResults | Notification: scheduled scan results | true | @AppStorage (UserDefaults) | Bool | SettingsView.swift toggle; NotificationService.swift gates the 'scheduled scan found meaningful reclaimable space' notification | When off, a completed scheduled scan never triggers the reclaimable-space notification. | no | always | NotificationServiceTests.swift, generate-settings-matrix.py --check (repository-doctor gate) | VERIFIED_COMPLETE |
| notif.enabled.storageGrowth | Notification: storage growth | true | @AppStorage (UserDefaults) | Bool | SettingsView.swift toggle; NotificationService.swift gates the 'tracked storage grew since the previous comparable scan' notification (delivered coalesced with scan results) | When off, a scheduled scan never triggers the storage-growth notification. | no | always | NotificationServiceTests.swift, generate-settings-matrix.py --check (repository-doctor gate) | VERIFIED_COMPLETE |

Total settings: 9
