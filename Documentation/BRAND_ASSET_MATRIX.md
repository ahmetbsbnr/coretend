# Brand Asset Matrix

Every current brand-specific asset, real paths from this session's
inventory — not a guess at what "should" exist.

## Currently generated (source of truth: `Resources/Brand/Sources/generate-brand-assets.swift`)

| Asset | Path | Sizes | Regeneration |
|---|---|---|---|
| App icon (all sizes) | `Resources/Brand/Generated/AppIcon.iconset/icon_{16,32,128,256,512}x{16,32,128,256,512}[@2x].png` | 16–1024 | Regenerated by `generate-brand-assets.swift`, not hand-edited per size |
| App icon (compiled) | `Resources/Brand/Generated/AppIcon.icns` | — | `iconutil` over the iconset, per the generator script |
| App icon (flat PNG) | `Resources/Brand/Generated/AppIcon-1024.png` | 1024 | Same generator |
| Menu bar template icon | `Resources/Brand/Generated/MenuBarTemplate.png` / `@2x.png` | 1x/2x | Same generator — must stay a template image (`isTemplate = true` at load, per `MENU_BAR` code) so it tints correctly in light/dark menu bars |

## Not yet present (deliverables for a future logo, once a name is approved)

| Deliverable | Purpose | Notes |
|---|---|---|
| Favicon | Website | `Website/` currently has no dedicated favicon asset found this pass — check during implementation, don't assume |
| Open Graph image | Website social previews | `professionnel-portfolio-stage-2026/app/opengraph-image.png` is the portfolio's own equivalent — product site needs its own, not a copy |
| Twitter/X card image | Website social previews | Same as above |
| Horizontal logo (wordmark + mark) | Documentation headers, README, external listings | Not yet created |
| Compact/icon-only mark | Small-space usage (favicons, app-icon-adjacent contexts) | The app icon itself may satisfy this, or a simplified derivative may be needed |
| Light-mode logo variant | Light backgrounds | — |
| Dark-mode logo variant | Dark backgrounds | — |
| Monochrome variant | Single-color contexts (stamps, engraving-style usage, print) | — |
| Vector source files | Editable master files (the current generator is a Swift script producing raster output — no `.svg`/`.sketch`/`.fig` source was found this pass; worth deciding on a real vector source of truth during implementation) | — |
| DMG background/branding | `Scripts/package-dmg.sh` — currently text/layout only, no custom background image found this pass | — |
| Onboarding imagery | Already uses the static `CoreBloomMark` per `INSTALLER_EXPERIENCE.md` — carries forward unless the visual brief changes this |
| Documentation header images | None currently found — optional future addition |

## Rule for all of the above

None of these are produced this phase. Every one is blocked on the same
gate as the rename itself: `BRAND_NAME_CLEARANCE.md` status must reach
`CLEAR_FOR_ENGINEERING` and
`Configuration/BrandRenameApproval.local.json` must exist. See
`LOGO_MIGRATION_PLAN.md` for what happens once that gate opens.
