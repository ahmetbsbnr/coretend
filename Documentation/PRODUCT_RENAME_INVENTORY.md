# Product Rename Inventory

Generated by real `git grep` counts over the tracked tree at HEAD
`b9cb61d`, not hand-typed. Nothing renamed — this is the map a future
rename would need to walk. See `product-rename-inventory.json` for the
machine-readable version and `PRODUCT_RENAME_PLAN.md` for what to do with
each category.

## Raw pattern counts (tracked files, binaries excluded)

| Pattern | Files | Matching lines |
|---|---|---|
| `MacCare Local` (display name) | 126 | 328 |
| `MacCareLocal` (no space — identifiers) | 33 | 71 |
| `MACCLEAN` (repo dir name / package name in prose) | 10 | 21 |
| `mac-care-local` (planned repo-URL slug) | 6 | 7 |
| `local.maccare.app` (bundle identifier) | 10 | 14 |
| `maccare.ahmetbsbnr...` (planned website URL) | 4 | 4 |

## By category

### Bundle identifier / executable / process name
- `local.maccare.app` — `Resources/Info.plist` (CFBundleIdentifier),
  `Configuration/PublicIdentity.example.json` (`bundleId`),
  `Scripts/check-version-consistency.sh` (reads/verifies it),
  `Documentation/` (referenced in several audit/compliance docs).
- `MacCareLocal` — `Package.swift` (package name **and** executable
  target name), `Resources/Info.plist` (CFBundleExecutable), process name
  visible in `ps`/Activity Monitor, `Sources/MacCareApp/AppEnvironment.swift`
  and `Sources/Persistence/Store.swift` (data-path construction — see
  `USER_DATA_RENAME_MIGRATION.md`).

### SwiftPM target/module names
- Executable target: `MacCareApp` (contains "MacCare" but is a generic
  module-naming pattern, not itself user-facing).
- Test target folder: `Tests/MacCareAppTests/` — literal directory name
  tied to the SwiftPM target-naming convention (`<Target>Tests`).
- Library targets (`ScanCore`, `SafetyCore`, `FileRules`, `DesignSystem`,
  `Persistence`, `SystemMetrics`, `AppDiscovery`, `MalwareEngine`) —
  **not** brand-coupled, no rename needed regardless of product name.

### Local data paths (SQLite, quarantine, diagnostics)
- `~/Library/Application Support/MacCareLocal/` — parent directory for
  `store.sqlite` (settings/exclusions/activity tables) and `Quarantine/`
  (malware-scan quarantine). Full migration strategy in
  `USER_DATA_RENAME_MIGRATION.md` — this is the highest-risk category
  since it's real user data on disk, not just source text.
- Diagnostics export (`Sources/MacCareApp/DiagnosticReport.swift`) embeds
  the product name in its generated report header/filename — text-only,
  no persisted state.

### UserDefaults / preference keys
- `menuBarEnabled`, `onboardingDone`, `onboardingStep` — key names
  themselves are generic (no "MacCare" substring), but they live under
  the app's `NSUserDefaults` domain, which macOS keys by
  **CFBundleIdentifier** (`local.maccare.app`) — so a bundle-ID change
  silently orphans these prefs unless migrated. See
  `USER_DATA_RENAME_MIGRATION.md`.

### Menu bar
- `Sources/MacCareApp/MacCareApp.swift` / menu-bar view — menu title and
  `MenuBarTemplate.png`/`MenuBarTemplate@2x.png` asset names reference no
  brand string directly in the filename, but the menu's visible title
  text is "MacCare Local" (data-driven from `CFBundleName`, not
  hardcoded — a bundle-name change updates this automatically).

### URL schemes / Launch at Login / notification categories
- **None found.** No `CFBundleURLSchemes`, no `SMAppService`/login-item
  code, no `UNNotificationCategory` identifiers exist in `Sources/` today
  — nothing to migrate in these three categories specifically.

### Scripts
- 12 files under `Scripts/` reference "MacCare Local" in comments/output
  strings (doctor.sh, repository-doctor.sh, package-*.sh, build-release.sh,
  etc.); 6 reference `MacCareLocal` directly (executable name, paths).
  These are safe, low-risk text substitutions — no data-migration
  implication.

### ZIP / DMG / release manifest
- `Release/latest.json` — `product`, `zipName`, `dmgName` fields.
- `Release/Notes/*.md` — release notes reference the product name in
  prose (not machine-parsed, low risk).
- `Scripts/build-release.sh`, `package-zip.sh`, `package-dmg.sh` —
  construct artifact filenames from a version argument, not a hardcoded
  brand string in most cases; the DMG volume name and window title
  (`package-dmg.sh`) do hardcode "MacCare Local".

### Website / site content
- 30 files under `Website/` (mostly `generate.py`'s templated output +
  the generator source itself) reference "MacCare Local" — entirely
  regenerated from `Website/generate.py`, so this is a single
  source-of-truth edit + regenerate, not 30 manual edits.

### Documentation
- 48+20+9 = 77 files across `Documentation/` reference some form of the
  name — the largest single category by file count, but almost entirely
  prose (low functional risk, high volume of manual review needed).

### Licenses / legal / trademark
- `TRADEMARKS.md`, `NOTICE`, `COPYRIGHT`, `LICENSE`,
  `THIRD_PARTY_NOTICES.md` — each references "MacCare Local" once, in a
  legal-text context. These need careful, deliberate review at rename
  time (not a blind find-replace), since they are the actual governance
  documents for whatever name is chosen.

### CI workflows
- `.github/workflows/ci.yml`, `compat-matrix.yml`, `release-draft.yml`,
  `dependabot.yml`, plus all 6 `.github/ISSUE_TEMPLATE/*.yml` files
  reference the product name in titles/labels — text-only, no functional
  coupling to the bundle identifier.

### Fixtures / test names / comments
- Test fixtures use generic temp-directory naming (`maccare-*-\(UUID())`
  patterns in several `Tests/*.swift` files) — these are arbitrary,
  collision-avoidant temp names, not brand-load-bearing; renaming them is
  cosmetic, zero functional risk.

## What's explicitly NOT in this inventory

Core Bloom / Orbital Ecology design-system names (`MCColor`, `MCSpacing`,
`MCMotion`, `MCFont`, `CoreBloomMark`, etc.) are **not** brand-name
references — they are the design-language's own vocabulary, independent
of the product's marketing name, and are explicitly preserved per
`REBRAND_VISUAL_BRIEF.md`.
