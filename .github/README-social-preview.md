# Repository social preview

`social-preview.png` (1280×640) is the image GitHub shows when the repo is
linked on social media / chat. It is **not** applied automatically — upload it
once in **Settings → General → Social preview**. Regenerate it from
`Resources/Brand/Sources/generate-brand-assets.swift` output whenever the
brand marks change.
