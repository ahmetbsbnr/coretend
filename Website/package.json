{
  "name": "coretend-site-api",
  "version": "0.1.0",
  "private": true,
  "description": "CoreTend public website \u2014 Contact & Community API (Vercel Functions).",
  "license": "Apache-2.0",
  "type": "commonjs",
  "scripts": {
    "test": "node --test test/*.test.js",
    "migrate": "node scripts/migrate.mjs"
  },
  "dependencies": {
    "@vercel/postgres": "^0.10.0"
  },
  "engines": {
    "node": ">=20"
  }
}
