# Pinned so a DMG built today and a DMG built next year from the same commit
# lay out identically. dmgbuild writes the .DS_Store through ds-store/mac-alias
# — no Finder, no AppleScript, no Automation grant, no GUI session.
dmgbuild==1.6.7
ds-store==1.3.1
mac-alias==2.2.2
