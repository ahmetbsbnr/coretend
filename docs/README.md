Decision documents for the frontend rebuild. Start with
`INFORMATION_ARCHITECTURE.md`, then `DESIGN_SYSTEM.md`; execute from
`REMAINING_WORK.md`. `Documentation/` holds the older research and audits.
