# Bundle Inventory — MacCare Local 0.8.0

Generated by inspecting the actual built artifacts (`unzip -l`, `find`), not
hand-typed. See `Release/latest.json` for checksums/sizes.

## ZIP contents (`MacCare-Local-0.8.0-arm64-unsigned.zip`)

```
MacCare Local.app/
MacCare Local.app/Contents/
MacCare Local.app/Contents/_CodeSignature/CodeResources
MacCare Local.app/Contents/MacOS/MacCareLocal
MacCare Local.app/Contents/Resources/MenuBarTemplate.png
MacCare Local.app/Contents/Resources/AppIcon.icns
MacCare Local.app/Contents/Resources/MenuBarTemplate@2x.png
MacCare Local.app/Contents/Resources/MacCareLocal_MacCareApp.bundle/Localizable.xcstrings
MacCare Local.app/Contents/Resources/MacCareLocal_MacCareApp.bundle/Base.lproj/Localizable.strings
MacCare Local.app/Contents/Resources/MacCareLocal_MacCareApp.bundle/fr.lproj/Localizable.strings
MacCare Local.app/Contents/Resources/MacCareLocal_MacCareApp.bundle/Info.plist
MacCare Local.app/Contents/Info.plist
LICENSE
NOTICE
THIRD_PARTY_NOTICES.md
```

21 entries (files + directories), 7,619,111 bytes uncompressed.

## DMG contents (`MacCare-Local-0.8.0-arm64-unsigned.dmg`)

```
MacCare Local.app/
Applications  (symlink to /Applications, drag-to-install)
LICENSE
NOTICE
THIRD_PARTY_NOTICES.md
```

No privileged install scripts, no `postinstall` hooks, no personal data.
Mounted and verified this build: contents match the above exactly.

## App bundle structure (`MacCare Local.app/Contents/`)

- `Info.plist` — `CFBundleShortVersionString`/`CFBundleVersion` = 0.8.0,
  `LSMinimumSystemVersion` = 14.0, `CFBundleIdentifier` = `local.maccare.app`.
- `MacOS/MacCareLocal` — arm64 executable (confirmed via `file`/`lipo` in
  `test-distribution.sh`), 6,218,608 bytes.
- `Resources/AppIcon.icns`, `MenuBarTemplate.png` / `@2x` (template image,
  monochrome menu-bar icon).
- `Resources/MacCareLocal_MacCareApp.bundle/` — SwiftPM resource bundle:
  `Info.plist`, `Localizable.xcstrings`, `Base.lproj/Localizable.strings`
  (EN), `fr.lproj/Localizable.strings` (FR). Both localizations ship.
- `_CodeSignature/CodeResources` — ad-hoc signature (unsigned distribution,
  no Developer ID — see `Documentation/INSTALL_UNSIGNED.md`).

## Localizations shipped
- English (Base)
- French (fr)

## Architecture
- arm64 only (Apple Silicon). No x86_64 slice.

## What is NOT in the bundle
- No embedded ClamAV binary or virus signatures (optional, installed
  separately by the user — see `Documentation/CLAMAV.md`).
- No telemetry/analytics SDK, no crash reporter, no network framework
  usage (`Documentation/PRIVACY.md`).
- No developer's real username or local absolute paths in the binary's
  live code path (`test-distribution.sh`'s repo-checkout-path check
  passes; the SwiftPM resource-bundle-accessor fallback string caveat is
  tracked in `Documentation/KNOWN_LIMITATIONS.md`, not a leak).
